# SPA fallback (keep client-side routing)
# Critical: NEVER fallback /assets/* to index.html (otherwise Vite chunks receive HTML → "Unexpected token '<'").
/assets/* /assets/:splat 200

# Everything else falls back to the SPA entry
/* /index.html 200
